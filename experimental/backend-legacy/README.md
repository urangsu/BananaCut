# Legacy Backend Experiment

This backend is a legacy experiment.
It is not used by the production BananaCut app.
Do not deploy it without adding authentication, CORS allowlist, upload size limits, rate limiting, timeout, and retention policy.
Production BananaCut is local-only and does not upload media files to a backend server.
